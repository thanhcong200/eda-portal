'use strict';

/**
 * ai-propensity-model router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::ai-propensity-model.ai-propensity-model');
